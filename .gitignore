# Third-party rankings exports. The repository is private, so this is no longer
# about disclosure — the saved backup already carries the parsed rankings, and
# the raw CSVs would only be a second copy. Drop this line if you want them
# tracked as a fallback.
*.csv

# Scratch files from testing
__*.html
__*.py
