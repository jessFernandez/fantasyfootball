# Raw rankings exports. The saved backup already carries the same data in
# parsed form, so tracking these too would only be a second copy. Drop this
# line if you want them here as a fallback.
*.csv

# Scratch files from testing
__*.html
__*.py
