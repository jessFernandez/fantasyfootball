# Third-party rankings exports. Kept out deliberately: this repository is
# public, and these are RotoBaller's projections — the "premium" file is
# subscriber content. Keep your own copies locally and import them in the app.
*.csv

# Scratch files from testing
__*.html
__*.py
